---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { t } from '../../i18n';

const i18n = t('en');
---

<BaseLayout title={`${i18n.contact.title} - ${i18n.site.title}`}>
  <h1>{i18n.contact.title}</h1>
  <p>{i18n.contact.expectation}</p>
</BaseLayout>
